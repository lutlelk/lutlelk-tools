# npm registry configuration
registry=https://registry.npmjs.org/

# Always authenticate when publishing
always-auth=true

# Use npm public access by default
access=public

# Save exact versions
save-exact=true

# Use package-lock.json
package-lock=false
